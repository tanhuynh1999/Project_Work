@model ProjectWork.Models.Comment


<h3 class="mb-5">Leave a comment</h3>
@using (Ajax.BeginForm("Create", "Comments", new AjaxOptions { UpdateTargetId = "NewFormComment", HttpMethod = "Post", OnSuccess = "LoadListComment()" }))
{
    @Html.AntiForgeryToken()
    int id = (int)ViewBag.employer_id;
    <!--<div class="form-group">
        <label for="name">Name *</label>
        <input type="text" class="form-control" id="name">
    </div>
    <div class="form-group">
        <label for="email">Email *</label>
        <input type="email" class="form-control" id="email">
    </div>
    <div class="form-group">
        <label for="website">Website</label>
        <input type="url" class="form-control" id="website">
    </div>-->
    <input type="hidden" name="employer_id" value="@id" />
    <div class="form-group">
        <label for="message">Message</label>
        <textarea name="comment_contents" id="message" cols="30" rows="10" class="form-control"></textarea>
    </div>
    <div class="form-group">
        @if (Session["member"] == null)
        {
            <input type="submit" data-toggle="modal" data-target="#login" value="Post Comment" class="btn py-3 px-4 btn-primary">
        }
        else
        {
            <input type="submit" value="Post Comment" class="btn py-3 px-4 btn-primary">
        }
    </div>
}